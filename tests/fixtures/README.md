# Test Fixtures

Use this folder for stable test fixtures shared by unit, integration, and e2e tests.

Current fixture sources:

- `fixtures/sample-inventory.json`
- `fixtures/sample-customers.json`
- `fixtures/seed-data.sql`
