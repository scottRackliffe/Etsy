# ADR & Supporting-Docs Audit — 2026-06-22

Full audit of all 85 ADRs (`documents/adr/`) and supporting documents for clarity, lack of
ambiguity, fitness for purpose, deprecated-but-not-removed content, and missing specificity.

This file is the **working findings log**. The final ordered fix list is at the bottom
(section "ORDERED FIX LIST"), built up as the audit proceeds.

> **STATUS: REMEDIATION COMPLETE (2026-06-22).** All 69 findings (F1–F69) addressed across 9 commits
> on branch `docs/adr-audit-remediation` (clusters 0–6). The owner reviewed every finding (decision
> log below) and set five governing rules. Code was used as ground truth where it mattered (publish
> fields F29, thumbnail F8, report removals F59, tab list F61). **One item consciously deferred:**
> F20 (ADR-018 Appendix A example-label ordering — purely cosmetic, no semantic impact). Final
> verification sweep: app-name, invalid enum, stale ADR counts, nav phrases, the "98" gate, and
> Coach/Workshop-as-current all clean in live docs.

---

## Review decisions (owner walkthrough, 2026-06-22)

Calls made while reviewing line-by-line. "✓ do" = apply as written; details note any nuance.

| Finding | Decision |
| --- | --- |
| F1 | ✓ do — update README to **85**. |
| F2 | (pending) default: update to 85 **+ "(as of <date>)"** since it's an audit snapshot. |
| F3 | **Delete** the duplicate `TrudysClassicTreasures/etsy-sales-manager/docs/adr/` set. |
| F4 | **Delete** `documents/adr.zip`. |
| F5 | **Archive** the retired Coach docs (`LISTING_COACH_SCOPE.md`, `Listing_Coach_Guide.md`) and **fix inbound links** — *except* linkers that are themselves being archived/rewritten (e.g. `listing-authoring-task-cards.md` is a Coach build doc → archive too; tutorial / KB-catalog / listing-template are rewritten in Phase 1 so their links drop anyway). |
| F6 | **Fix** — normalize Status to the keyword **and improve clarity/detail** on the separate line. |
| F7 | **Repoint, do not archive** — ADR-007 is live/foundational; update the scope-matrix `listings_w`/`listings_r` rows from ADR-023 → **ADR-073 §2.4** (scope catalog) + ADR-085 (publish). |
| F8 | **Aspect-preserving is canonical** → the **SSOT (ADR-026 §5) is wrong** and must change from `fit: cover` / 200×200 crop to **`fit: inside`, max dimension 200** (aspect-preserving); ADR-002's inline wording was correct. Align both. |
| F9 | **Four actions canonical** (Print \| Export PDF \| Export CSV \| Cancel — the built `ReportViewer`). Fix ADR-013 line 23. |
| F10 | **Confirmed — browser-print model**; remove the server-side PDFKit 500 path from ADR-013 edge cases (PDFKit retained only for the batch print queue). |
| F11 | ✓ fix markdown escaping + de-dupe schema-map rows. |
| F12 | ✓ trim stale "last 20/limit=20" from the superseded paragraph. |
| **Governing principle** | **"No ambiguity is Job 1."** Inaccurate or conflicting information = ambiguity → **fix it**, don't "leave / mark historical." This resolves borderline calls toward active correction (e.g. F2 → just set 85; F17 → add the indexes so narrative and DDL agree; F32 → actually clarify superseded bodies, not just banner them). |
| F13 | ✓ renumber the duplicate `5f` (narrative + DDL comments). |
| F14 | **Fix** → replace invalid `before_2004` with **`2004_2009`** (consistent with §1a "vintage = `2004_2009` or earlier"). |
| F15/F20 | ✓ repoint retained-column refs (`picture_classifications`, `listing_category_path`, six `listing_*` strategy fields) from ADR-072/023 → ADR-085 + taxonomy's new home (F21). |
| F16 | ✓ reorder DDL so each table precedes its indexes. **Verify the real migration/bootstrap (`src/lib/sqlite.ts` + `migrations/`) shares/avoids the bug when applying.** |
| F17 | **Add** the missing indexes (`idx_orders_shipper`, `idx_vendors_is_active`) to the §8 DDL so it matches §7 (no-ambiguity: keep the useful intent). |
| F18 | ✓ add a narrative table section for `communication_log`; sanity-check the §-number ordering of COA/GL/tax/receipts/expenses. |
| **Governing rule 2** | **Listing Workshop is fully removed** (it was the other AI-assisted "improve the quality score" path) **and the Coach is removed** — including from **code**. Every "Listing workshop" / "ListingAuthoringPanel" / "PublishPreview" / mode-toggle / `openWorkshop` / draft→approve reference is stale → **remove**. Applies to F23, F30, F48, F54, F60, F61, F65, F66, F69. |
| **Verify (code, not docs)** | Confirm WS-L6 actually removed from source: `/listing-coach` route, `ListingAuthoringPanel`, `PublishPreview`, `src/lib/listing-coach.ts`, `improve-listing`. The volume of stale doc refs suggests the code removal may be incomplete — docs and code must agree. |
| F21 | **ADR-083 = canonical home** for the shot-type taxonomy (per recommendation); ADR-082 §8a and all others reference ADR-083. |
| F22 | ✓ drop "Listing Coach (ADR-072)" UI surface from ADR-083/084; keep inventory detail picture area. |
| F23 | **Remove** the `openWorkshop=1` deep-link param entirely (workshop gone; no replacement — listing content is in the SEMS inventory detail). |
| F24 | ✓ delete the superseded "Listing workshop (manual/AI/portable)+approve" row from ADR-070 scope matrix. |
| F25 | ✓ mark ADR-023 as superseded in ADR-073's cross-ref list. |
| F26 | ✓ collapse ADR-023's double status line to one. |
| F27 | ✓ update Type 8 prose to `listing_phase IN (...)` to match its own SQL. |
| F28 | ✓ reorder ADR-020 body so Type 7 precedes Type 8. |
| F29 | **Resolved by code** (`validatePublishReadiness`, `src/lib/inventory-validation.ts:93`): blocking = `etsy_when_made`, `etsy_taxonomy_id`, `etsy_return_policy_id`; `etsy_shipping_profile_id` = **warning**; `etsy_who_made` = **not gated** (from global default). → **Fix ADR-017 §1c** (shipping_profile → recommended/warning; who_made → defaulted, not gated). **ADR-021 §8**: keep 3 blocking, **add shipping_profile as warning row**. Code observation (optional): who_made unvalidated → publish could fail at Etsy if global default cleared. |
| F31 | ✓ fix "Type 9" → "Type 8" in ADR-072 if touched. |
| F32 | **Banner-in-place** — keep 023/068/072 in the numbered set; add an unmissable "⚠️ SUPERSEDED — historical only, do not implement (see ADR-085/082)" banner at the Decision/API sections (archiving would leave gaps in the numbered sequence and break ADR-085's supersession trail). |
| **Governing rule 3** | **The report list is fluid; the most recent ADR wins.** When ADRs conflict on report scope, the latest dated statement is authoritative. Clean the base so it's unambiguous; new reports get added to a clean base later. |
| F33 | ✓ trim design-decisions §8 to a pointer (four-action set already correct). |
| F34/F58 | ✓ replace all stale "12pt Courier / page numbers centered" report-layout refs (design-decisions §21, ADR-036/038/039/054) with a bare "per ADR-013" pointer. |
| F35 | ✅ **applied** — implementation-guide listing-authoring phase rewritten around ADR-085/081/082; ADR-023/Coach/Workshop/approval-gate refs removed; "Where to look" row updated. |
| F37 | ✅ **applied** — implementation-guide scope note added (Phases 1–7 baseline; later work in PROGRAM_2026-06-21 + tickets). |
| F38 | ✓ rewrite tutorial.md + KB-catalog + etsy-listing-template around the ADR-085 lifecycle (user-facing, high priority). |
| F59 | **Latest ADR wins (rule 3)** → income-MTD, income-YTD, postal-by-vendor **removed** (per ADR-036 2026-06-17). Clean ADR-006 / ADR-013 / ADR-018 §9 to match. **Consequence: ADR-005** (postal-by-vendor report) is folded into Costs → give it the F32 superseded banner. |
| F40 | ✓ ADR-082 cite the live setting `listing.min_quality_score` for the gate, not the retired ADR-068. |
| F41 | ✓ ADR-082 §8a shot taxonomy → ADR-083 (per F21). |
| **F42 — RESOLVED (canonical quality model)** | **85 is a firm hard gate, not a climbing target.** No item publishes to Etsy with quality < `listing.min_quality_score` (default **85**); 85 is the minimum, some items legitimately top out there, publishing at exactly 85 is allowed, and the gate does **not** auto-climb. The AI (Generate + Evaluate Quality) continually helps raise the score **toward ~100** (the aspiration), but exceeding 85 is **not required** to publish. **Delete the "98" advisory entirely** (third number = ambiguity) and **remove ADR-016's "threshold will rise toward ≈98%"** (the gate is fixed, not rising). Propagate the two-number model (gate 85 + aspiration ~100) to ADR-082, ADR-081, ADR-085 §4, ADR-016. |
| F43 | ✓ ADR-079 status → completed rollout (E2–E6 done). |
| F44 | ✓ ADR-030 reconcile to SEMS (governing rule 1); keep field/validation tables. |
| F45 | ✓ ADR-031 reconcile to SEMS; keep field defs. |
| F46 | ✓ ADR-031 endpoint `thank-you` → `thank-you-note`. |
| F47 | ✓ ADR-030 shot-taxonomy ref → ADR-083 (per F21). |
| F48 | ✓ ADR-024 §3.3/§5 — remove Listing Workshop panel, `ListingAuthoringPanel`, `PublishPreview`, modes/approve (rule 2); reconcile to single inventory detail + lifecycle button. |
| F49/F61 | **Canonical tab bar RESOLVED FROM CODE** (`src/components/shell/TabBar.tsx`, 13 tabs): **Dashboard · Orders · Shipping · Inventory · Receipts · Customers · Communications · Vendors · Expenses · Reports · Outstanding · Tutorial & tips · Settings.** Reconcile ADR-009/024/076/080 + ui-design to this. **Note:** every ADR tab list omits **Communications** and several mislabel **Orders** as "Sales" and **Settings** as "Config". |
| F50 | **Resolved by code** — route/label is **`/settings` "Settings"**, not `/config`. ADR-024 §3.3 path and "Config" naming align to code. |
| **Governing rule 5 (product name) — RESOLVED** | **The application is named `AiCE` — "The AI-Powered eCommerce Engine"** (an AI engine for world-class listings across e-commerce platforms; platform-general by design — Etsy is the first/current integration). Replace every app/product/title reference ("Etsy Sales Manager", "Trudy's Etsy Sales", "Trudy's Sales Manager", etc.) with **AiCE**. **Keep** "Etsy" where it means the marketplace/API, and **keep** "Trudy's Classic Treasures" — it is the **first Etsy store using AiCE** (real pilot shop; stays as example / business-profile data). Per ADR-073, "Etsy" must not be in the app *name* anyway. |
| **Governing rule 4 (naming) — RESOLVED** | **The menu tabs are the canonical taxonomy; docs align to code.** **"Sales" → "Orders"** and **"Config" → "Settings"** everywhere they name the nav tab/section (e.g. ADR-034 title "Config completion" → "Settings completion"; "Sales tab" → "Orders tab"). "Config" and "Sales" are deprecated terms like Coach/Workshop. **Care:** rename only nav/section references — keep legitimate domain terms (`sale_revenue`, "Sales Tax Summary" report name, "customer sales" concept, "configurable"). Affects F50/F61 and many ADRs (009, 016, 020, 031, 034, 044, …) + ui-design. |
| F51/F57 | ✓ reconcile Config section count (ADR-034 "22" vs 20 listed; ADR-024 "8"; ADR-027 "8") to the **actual rendered sections** in the settings page (verify count when applying). |
| F52 | ✓ Resolved by F8 — fix the **ADR-026 SSOT** to aspect-preserving (`fit: inside`, max 200); ADR-002 was right. |
| F53 | ✓ ADR-026 §8 add "deferred post-v1" directory-import caveat; fix §9→§10 cross-ref. |
| F54 | ✓ ADR-032 — remove Reject/Approve-draft entries (rule 2); unsaved-changes guard → 3-button SEMS dialog (ADR-042/079). |
| F55 | ✓ ADR-037 API example → nested pagination envelope. |
| F56 | ✓ ADR-037 §"DB schema" `entity_type` list (line 52) → authoritative §A1 set. |
| F60 | ✓ ADR-039 fix "ADR-034 §2b" cross-ref to the real section. |
| F62 | (method) Use each WS-ADR's own "cross-references to update when implemented" checklist as the remediation backlog. |
| F63 | ✓ ADR-076 Vendors "master-detail" → SEMS (it's the SEMS pilot); tab list → canonical (F61). |
| F64 | ✓ ADR-076 "Listing Coach" VendorPicker usage → inventory detail / inline create (rule 2). |
| F65 | ✓ ADR-042 "listing workshop" label → consolidated Listing Content (rule 2). |
| F66 | ✓ ADR-060 user-facing tooltips — rewrite `listing_draft_state`/workshop tooltips for `listing_phase` lifecycle (rule 2; user-facing, high priority). |
| F67 | ✓ ADR-061/071/077 — "master-detail / two-panel / workshop" → SEMS; ADR-077 state Expenses is on SEMS. |
| F68 | ✓ ADR-075 — reconcile active `listing-coach` examples (13/35/47/150) with its own retirement note (line 75). |
| F69 | ✓ ADR-071 §4.3 `listing_draft_state` → `listing_phase` (rule 2). |

---

## Raw findings (as discovered)

### Structural / cross-cutting

- **F1** — `documents/README.md:30` says "**71 ADRs**". Actual = **85**. Stale count.
- **F2** — `documents/DOC_FUNCTIONAL_UX_COVERAGE_AUDIT.md:29` says "**69 ADRs**". Stale count.
- **F3** — Duplicate ADR set at `TrudysClassicTreasures/etsy-sales-manager/docs/adr/` holds
  0001–0008; **all 8 differ** from canonical `documents/adr/`. Second source of truth / stale fork.
  Its README still describes ADR-008 with the old "reports not stored" wording.
- **F4** — `documents/adr.zip` is a frozen 2026-02-23 snapshot of only 23 ADRs. Misleading stale
  artifact living in the docs tree.
- **F5** — ADR-085 Notes lists `documents/LISTING_COACH_SCOPE.md` and
  `system/tips/Listing_Coach_Guide.md` as "Retired docs," but both still exist. ~5 other docs still
  reference the removed Listing Coach (no-developer-questions-build, knowledge-base-topics-catalog,
  etsy-listing-template-and-requirements, tutorial, listing-authoring-task-cards).
- **F6** — Status vocabulary drift vs the ADR format spec (`Accepted | Deprecated | Superseded`).
  Real values include `Implemented (WS-F…)` (080), `Accepted — scaffold built…` (079),
  `Accepted (updated …)` (034), `Accepted — implemented (WS-H1…)` (083/084),
  `Accepted — made canonical by ADR-085…` (081). Inconsistent; status should be a single keyword
  with detail moved to a clarification line.

### Per-ADR

- **F7** — ADR-007 OAuth scope matrix (lines 27–28) cites the **superseded ADR-023** for
  `listings_w` / `listings_r` ("Write/Read listings — ADR-023"). Should reference ADR-085 (and/or
  ADR-073, the consolidated Etsy API reference).
- **F8** — ADR-002 line 25: thumbnail spec stated inline ("JPEG, max dimension 200 px, stored at
  thumbnail_path"); `thumbnail_path` not code-formatted like neighbors. Verify it matches ADR-026
  (the canonical thumbnail spec) — duplicated spec risks drift.
- **F9** — ADR-013 internal contradiction on post-report actions. Line 23 says "**exactly three
  actions: Print / Save as PDF, Export CSV, Close**"; edge-case #4 (line 127) refers to "the
  standard **four-action flow (Print | Export PDF | Export CSV | Cancel)**." Two different canonical
  action sets in one ADR. Pick one and make both spots agree.
- **F10** — ADR-013 deprecated-but-not-removed: Generation section (line 21, updated 2026-06-17)
  states "Server-side PDFKit generation has been removed from regular reports" (PDF now via browser
  print). But edge-case #2 (line 123) still specifies a server-side **PDFKit 500** failure path for
  reports, and edge-case #1/#2 still say "the PDF displays…". Stale relative to the HTML/browser-print
  model. Reconcile.
- **F11** — ADR-013 markdown corruption hurting an "exact / no ambiguity" spec: invoice section
  (line 61) renders `orders.ship_to*\*`, `business*name` as italics (stray asterisks); the schema
  map (lines 172–175) has the `ship*to*\*` italics bug **and duplicated rows** ("ship-to fields" /
  "sum of purchase.shipping_cost by shipper" appear twice). Clean the escaping and de-dup.
- **F12** — ADR-016 line 121: Activity-feed widget is correctly annotated "(Superseded by the
  2026-06-21 Activity views block below…)" but still states the old "last 20" / `limit=20` inline,
  which §6 contradicts (25). Trim the superseded inline figures to avoid a stale number.
- **F13** — ADR-017 has a **duplicate `### 5f.` heading**: §5f "Table: `activity_log`" (line 337)
  and §5f "Supporting tables" (line 354) are both numbered 5f. The DDL comments repeat it
  (`-- 5f. activity_log` line 858, `-- 5f. Etsy receipt cache` line 870). Renumber.
- **F14** — ADR-017 invalid enum example. The `etsy.publish.default_when_made` example value is
  `before_2004` (line 441) and §1c references the same key, but the **canonical `when_made` enum**
  in §1a (line 118) does not contain `before_2004` (valid values are `2004_2009`, `2000_2003`, …,
  `before_1700`). An invalid value used as the documented default in a "no-ambiguity" schema. Fix to
  a real enum value (e.g. `2000_2003`) or define the intended fallback.
- **F15** — ADR-017 stale source refs on **retained** columns: `picture_classifications` cites
  "ADR-072 §Photo classification" (ADR-072 removed by ADR-085); `listing_category_path` and the six
  `listing_*` strategy fields cite "(ADR-023)" (superseded by ADR-085). Update to ADR-085/083/082.
- **F16** — ADR-017 **§8 DDL is not runnable as written.** `CREATE INDEX … ON tax_payments`,
  `… ON receipts`, `… ON business_expenses` (lines 978–982) appear **before** those tables are
  created (lines 1012, 1026, 1052). Executed top-to-bottom, SQLite errors "no such table." The ADR
  bills this as the "canonical… exact, no ambiguity" DDL implementations must match — reorder so
  every table precedes its indexes.
- **F17** — ADR-017 §7 index narrative ↔ §8 DDL mismatch. §7 (line 591) says `orders` is indexed on
  `shipper` (postal-by-vendor) and §7 (line 600) says `vendors` on `is_active`, but the DDL creates
  neither `idx_orders_shipper` nor `idx_vendors_is_active`. Reconcile the two so the "single
  canonical schema" is internally consistent.
- **F18** — ADR-017 `communication_log` (§6h) is defined only in the DDL (line 1086); there is no
  narrative table section for it under §1–§7, yet the ADR states "§1–§7 narrative tables match this
  DDL." Add a narrative entry (or an explicit exception note). Also `chart_of_accounts`,
  `gl_transaction_rules`, `tax_payments`, `receipts`, `receipt_items`, `business_expenses` are
  narrated out of numeric order relative to the DDL — verify the §x labels are intentional.
- **F19** — Three-way disagreement on the **post-report action set** (extends F9). ADR-013 line 23:
  "exactly **three** actions: Print / Save as PDF, Export CSV, Close." ADR-013 line 127: "the
  standard **four-action** flow (Print | Export PDF | Export CSV | Cancel)." ADR-018 line 246:
  "Print, Export PDF, Export CSV, Cancel" (four). Also "Save as PDF" vs "Export PDF" and "Close" vs
  "Cancel" wording differ. Define one canonical set and propagate to ADR-013 §Generation, ADR-013
  edge case #4, and ADR-018 §9.
- **F20** — ADR-018 Appendix A example numbering is scrambled: "13) Concurrent edit" (line 478)
  precedes "12) Report endpoint response behavior" (line 493); Appendix B jumps B21 → B29. Cosmetic
  but this is a "no ambiguity" implementer reference. Renumber.
- **F21** *(important)* — The **shot-type photo taxonomy** (the 11-value enum: hero, angle, detail,
  backstamp, scale, imperfection, underside, grouping, lifestyle, measurement, extra) is canonically
  defined in **ADR-072 "§Photo classification" — which ADR-085 removed.** Yet it is cited as the
  live source of truth by ADR-017 (`picture_classifications`), ADR-030 (line 107), ADR-033 (line 76),
  ADR-083 (lines 28/37/56/72/148/161), and ADR-084 (lines 29/131). The single source of truth for an
  actively-used enum points at deleted content. **Relocate the taxonomy into a live ADR (083 or 085,
  or the rubric 082) and repoint every reference.**
- **F22** — ADR-083 (line 93) and ADR-084 (line 88) describe "Listing Coach (ADR-072)" as a current
  UI surface ("on the inventory detail picture area … and in Listing Coach"). The Coach is removed
  (ADR-085). Drop the Coach surface; keep the inventory detail picture area (ADR-030/033).
- **F23** — ADR-035 deep-link table (line 27): `openWorkshop=1` "automatically open the listing
  workshop panel (ADR-030, ADR-072)." The **Listing Workshop panel was removed** (consolidated into
  `InventoryDetailPanel` per ADR-023's 2026-06-16 note) and ADR-072 is removed. Stale deep-link
  param + target; reconcile with the current inventory-detail listing section.
- **F24** — ADR-070 product-scope matrix (line 40) still lists the **superseded** row "Listing
  workshop (manual / AI / portable) + approve + publish | v1 | ADR-023, 011" right next to the
  current row (line 41) citing ADR-085/081/082. The old row references the retired approve step and
  the superseded ADR-023 with no supersession note — contradicts the actual v1 scope. Remove or mark
  superseded.
- **F25** — ADR-073 reference list (line 592) cites "ADR-023: Listing content generation modes" with
  no superseded marker; minor, but this ADR is billed as a consolidated reference.
- **F26** — ADR-023 Status block carries two status lines: "**Superseded by ADR-085…**" (line 5) and
  a leftover "Accepted (historical)" (line 16) below the note. Collapse to one unambiguous status.
- **F27** — ADR-020 Type 8 **definition** (line 101) still reads "listing draft in progress
  (`generated`, `imported`, or `approved`)" — `imported`/`approved` are retired `listing_draft_state`
  values (ADR-085). The SQL just below (line 107, "updated ADR-085") correctly uses `listing_phase
  IN ('generated','needs_quality_remediation','listing_ready')`. Prose definition and the summary
  table ("draft in progress") were not updated to match the query. Fix the prose.
- **F28** — ADR-020 orders **Type 8 before Type 7** in the body (line 99 then line 125), while the
  summary table and deferral table list 7 then 8. Reorder the body for a "no ambiguity" doc.
- **F29** *(cross-spec gap)* — ADR-021 §8 publish validation (lines 158–166) lists `etsy_when_made`,
  `etsy_taxonomy_id`, and `etsy_return_policy_id` as required-before-publish but **omits
  `etsy_who_made` and `etsy_shipping_profile_id`**, which ADR-017 §1c (line 132–138) marks "Required
  for publish: Yes." Two "no-ambiguity" specs disagree on the required publish field set. Add the two
  missing fields to ADR-021 §8 (or reconcile which list is canonical).
- **F30** *(reinforces F21)* — ADR-072's superseded note (line 15) explicitly states the photo shot
  taxonomy is "**now owned by ADR-082/083**," yet the canonical taxonomy table physically still lives
  only in the removed ADR-072 (§Photo classification, lines 202–217) and every live reference points
  there. The promised relocation was never executed. The fix for F21 is concrete: move that table
  into ADR-083 (and/or 082) and repoint ADR-017/030/033/083/084.
- **F31** — ADR-072 line 455 cites "**ADR-020 Type 9**" for the missing-era/category outstanding
  item, but ADR-020 defines it as **Type 8**. The type number shifted; the (historical) reference is
  stale. Low priority since ADR-072 is historical, but it signals the era/category type number is
  worth double-checking everywhere it's cited.
- **F32** — ADR-068 (superseded) still embeds the full live-tense rubric (max score 118) and the
  removed `GET /api/inventory/[id]/listing-score` endpoint without a "retired" inline marker on the
  endpoint/rubric body. Header is correctly marked; consider a one-line "for historical reference"
  banner at the Decision/API sections so a reader who lands mid-document isn't misled. (Same pattern
  applies to ADR-023 and ADR-072 bodies — present-tense specs under a superseded header.)

### Supporting documents

- **F33** *(feeds F19)* — `design-decisions-implementation.md` §8 (SSOT index) states reports offer
  "exactly **four** actions: Print, Export PDF, Export CSV, Cancel" and cites ADR-013 as SSOT — but
  ADR-013's actual Decision text says "exactly **three** actions: Print / Save as PDF, Export CSV,
  Close." The index's own rule ("Do not duplicate SSOT content… reference the canonical document")
  is violated, and the duplicate now contradicts the source. Four documents now disagree on this one
  control set (ADR-013 ×2 phrasings, ADR-018, this index).
- **F34** — `design-decisions-implementation.md` §21 "Report layout" summarizes "**12 pt Courier;
  title 14/16 pt; page number centered; header/footer every page; 1 in margins**." ADR-013's
  canonical layout is entirely different: **Crimson Text + Raleway fonts, brand banner image, cream
  alternating rows, 0.75in margins, no monospace, no page numbers.** Stale duplicated spec flatly
  contradicting its SSOT. Replace the §21 summary with a pointer (no duplication) or update it.
- **F35** — `implementation-guide.md` Phase 4 (lines 48–53) and the "Where to look for detail" table
  (line 100) still present the **superseded ADR-023 listing modes** (manual / integrated AI / hybrid
  export-import / **approval gate before publish**) as the plan, with **no reference to the canonical
  ADR-085 / 081 / 082** unified lifecycle. The flagship feature's implementation guide describes the
  removed workflow. Rewrite the listing-authoring phase around ADR-085.
- **F36** *(feeds F19)* — `implementation-guide.md` Phase 6 (line 68) repeats "Print, Export PDF,
  Export CSV, Cancel" — same four-action contradiction as F33.
- **F37** — `implementation-guide.md` predates the major-enhancements program: it has no phase for
  Vendors (ADR-076), Expenses (ADR-077), Shipping/EasyPost (ADR-074/080), SEMS (ADR-079),
  Communications (ADR-078), or the listing lifecycle/quality (ADR-081–085). As the stated
  "implementation order" doc it is materially incomplete. Either extend it or mark it
  "Phase 1–7 baseline only; see PROGRAM_2026-06-21 for later workstreams."
- **F38** *(highest user impact)* — **`tutorial.md` (built-in, user-facing) teaches the removed
  Listing Coach.** Line 45: "**Listing Coach** (Inventory → Add new listing with Listing Coach) lets
  you paste photos…"; line 66: "Use **Listing Coach** in the app… Review the draft before approving
  and publishing." The Coach was removed by ADR-085 (now inline SEMS create + lifecycle button), and
  the "approve before publish" step is retired. Users are told to use a non-existent feature. Also
  `knowledge-base-topics-catalog.md` D21–D25 (5 topics) and `etsy-listing-template-and-requirements.md`
  §7 ("Listing Coach (ADR-072)… the preferred path") and `listing-authoring-task-cards.md` §4 all
  present the Coach as current. Rewrite these around the ADR-085 inventory lifecycle and drop links
  to the retired `Listing_Coach_Guide.md`.
- **F39** *(sharpens F9/F19/F33/F36)* — The four-action report set **Print | Export PDF | Export CSV
  | Cancel** is the consistent convention across `ui-design.md` (lines 50/119), `frontend-architecture.md`
  (the real `ReportViewer`/`PdfPreview` component, lines 169/217), `implementation-guide.md`,
  `development-plan.md`, `design-decisions-implementation.md` §8, ADR-018 §9, and ADR-013's own
  edge-case #4. **The single outlier is ADR-013's main Decision line ("exactly three actions: Print /
  Save as PDF, Export CSV, Close").** So the fix is narrow and high-confidence: correct ADR-013 line
  23 to the four-action set (and reconcile the "Save as PDF/Close" wording) — not an open design
  question.

### Quality rubric (ADR-082) — the score engine

- **Positive** — ADR-082 is internally sound: category weights sum to 100 (40+15+15+10+10+5+5) and
  every category's sub-points add up (Title 3+4+3+3+2=15; Description 4+5+2+2+2=15; Tags 10; Cat&attr
  10; Condition 5; Pricing 5; Photos = 16 coverage + 24 per-photo = 40). No math errors found.
- **F40** — ADR-082 line 17 grounds the pass threshold in the **retired** ADR-068 it supersedes:
  "target around 98% (**pass threshold remains 85 per ADR-068**)." Self-undermining citation — §10 of
  the same ADR correctly anchors it to the setting `listing.min_quality_score` (default 85). Fix line
  17 to cite the setting, not ADR-068.
- **F41** *(reinforces F21)* — ADR-082 §8a Photos coverage table header is "Shot (**ADR-072 type**)."
  The canonical quality engine also sources the shot taxonomy from removed ADR-072. When the taxonomy
  is relocated (fix #16), ADR-082 §8a is both a referrer to repoint **and** a natural home.
- **F42** *(KEY AMBIGUITY — resolve per owner intent)* — three "good enough" numbers exist: **pass =
  85**, **advisory target = 98** (ADR-081/082/085), and the owner's stated aspiration **"near 100%."**
  Worse, ADR-016 §7 implies the *pass threshold itself* "will rise toward ≈98%," while ADR-082 keeps
  pass fixed at 85 with 98 advisory — unclear whether the gate climbs or stays. **Recommended
  canonical statement (matches the stated intent that quality is the gate and should approach 100):**
  publish floor = `listing.min_quality_score` (default **85**, configurable, *intended to rise as the
  catalog matures*); **advisory target = 98** shown in the UI; **aspiration ≈100**. State once and
  propagate to ADR-082, ADR-081, ADR-085 §4, ADR-016 §7.

### SEMS consolidation (ADR-079) — the second cleanup, same drift pattern

- **F43** — **ADR-079 status is stale.** It reads "Rollout to remaining entities (WS-E2..E6)
  **pending**," but `documents/tickets/completed/` holds WS-E2 (customers), E3 (receipts), E4
  (expenses), E5 (inventory), E6 (orders) — all **completed** — and git history shows "migrate Orders
  to SEMS scaffold (WS-E6)." The rollout is done; the status (and §6 plan) say it isn't.
- **F44** — **ADR-030 was never reconciled to SEMS, though ADR-079 flagged it "to be annotated."** It
  still specifies the **pre-SEMS** screen: a Modal create flow (line 177) instead of SEMS's "+ Add
  New" first-row inline editor; a list-then-detail-panel-below layout instead of the editor that
  *replaces* the list; and the **old two-button** dirty dialog ("unsaved changes. Discard them?",
  line 162) instead of SEMS's **three-button** Save/Discard/Keep. The listing section (193–201) *was*
  updated for ADR-079/085, so the ADR is internally split between SEMS-aware and pre-SEMS sections.
- **F45** — **ADR-031 same drift.** Annotated for the ADR-080 shipping split (good) but **never for
  ADR-079 (SEMS)**: still describes "master-detail layout: order list left/top, detail panel
  right/bottom" (lines 24–49) and a Modal create flow (line 204), both contradicting the SEMS
  scaffold (Add-New first row, editor replaces list, sticky Save bar, 3-button dialog).
- **F46** — ADR-031 line 174 calls `/api/reports/thank-you/{id}`, but the canonical endpoint is
  `/api/reports/thank-you-note/[orderId]` (ADR-013/018/036). Endpoint-name mismatch.
- **F47** *(reinforces F21)* — ADR-030 line 107 cites "ADR-072 §Photo classification" — another
  shot-taxonomy referrer to repoint.

### Routine UI/feature ADR sweep (line-by-line second pass)

- **Clean / strong (no findings):** ADR-025 (token refresh — exemplary), ADR-028 (shared
  components), ADR-029 (search/filter/sort/pagination), ADR-026 (picture storage — SSOT, solid).
- **F48** — ADR-024 half-migrated like ADR-030: its top notes (lines 58, 60) correctly point to
  SEMS/ADR-085, but **§3.3 Inventory (line 133) still lists the removed "Listing workshop" panel and
  `ListingAuthoringPanel` (Manual/Generate/Import mode toggle, approve/reject) + `PublishPreview`
  (approve gate)** — the retired ADR-023 modes + approve flow — and §5's file tree (line 271) repeats
  those dead components. Sweep §3.3/§5 to the single `InventoryDetailPanel` + lifecycle button.
- **F49** — ADR-024 §5 file tree (lines 220–234) is out of sync with §1 routing (lines 23–54): §5
  lists `sales/page.tsx` but §1 canonical is `orders/`; §5 omits the `shipping/`, `receipts/`,
  `vendors/`, `expenses/` tabs §1 has. Tab count also drifts: "9 tabs" (line 13) and "10 tabs"
  (line 69) vs **12** actual route segments in §1. Reconcile.
- **F50** — ADR-024 §3.3 gives the Config path as `src/app/(app)/settings/page.tsx` (line 167) but
  the route segment is `/config` (lines 50–51; §5 line 234). settings-vs-config inconsistency.
- **F51** — Config section count is stated three ways: ADR-034 header "**22 sections**" (line 21) but
  the table lists **20** (rows 1–20); ADR-024 §3.3 says Config has "**8** logical sections (ADR-034)."
  Reconcile to the real count.
- **F52** *(reinforces F8)* — ADR-002's inline thumbnail wording ("max dimension 200 px",
  aspect-preserving) differs from the ADR-026 SSOT ("200×200, `fit: cover` — crop to square, quality
  80"). Align ADR-002 to the SSOT or replace with a pointer.
- **F53** *(minor)* — ADR-026 §8 "Bulk import (directory → item)" omits the "deferred post-v1"
  caveat ADR-010 applies (browsers can't pick directories); and §9's cleanup cross-ref "(same as
  pictures per §9 below)" should read §10.
- **Clean / strong (no findings):** ADR-027 (backup), ADR-033 (image upload — correctly ports
  clipboard paste to ADR-085), ADR-035 (deep-link — well-updated for WS-A/D/F).
- **F54** *(SEMS + supersession drift)* — ADR-032 has stale entries: "**Reject listing draft**"
  (line 58) and "**Approve draft**" (line 78) reference the retired draft-state/approve flow
  (ADR-085); and the **Unsaved-changes guard** (lines 121–128) still specifies the **two-button**
  dialog ("Discard" / "Keep editing"), contradicting ADR-079 §4's **three-button** Save/Discard/Keep.
  Remove the reject/approve rows; update the guard to the SEMS 3-button dialog.
- **F55** — ADR-037 API response example (lines 188–206) uses the **flat** `{ items, total, limit,
  offset }` pagination shape, contradicting the canonical **nested** `{ items, pagination: {limit,
  offset, total, has_more} }` envelope (ADR-018 §3 / §B7, and ADR-029's own reconciliation note).
  Fix the example to nested.
- **F56** — ADR-037 §"Database schema" column def (line 52) lists a **stale/short** `entity_type`
  set (includes `listing`, which the authoritative §A1 set drops — listing.* actions use
  `inventory`) vs the canonical 15-value set in §A1 (line 289). Reconcile line 52 to A1 or point to
  it.
- **F57** *(ties F51)* — ADR-027 line 133 repeats the stale "Config page as one of **8 logical
  sections**" (ADR-034 has ~20). Same stale count as ADR-024 §3.3.
- **F23 cross-check** — ADR-035 line 27 (`openWorkshop=1` → "listing workshop panel") *directly
  contradicts* ADR-030 line 200 ("the `openWorkshop=1` query parameter is no longer used"). Confirms
  F23 with a same-corpus contradiction.
- **Clean / strong (no findings):** ADR-056 (accounting export — thorough, double-entry sound),
  ADR-069 (sample data — correctly updated for ADR-085).
- **F58** *(broadens F34 — systematic)* — The **retired "12 pt Courier, page numbers centered"
  report layout** (ADR-013 replaced it with Crimson Text + Raleway + brand banner) is still cited as
  "per ADR-013" in **ADR-036 (line 147), ADR-038 (line 141), ADR-039 (line 90)** and
  `design-decisions-implementation.md §21`. At least four docs propagate the old layout. Sweep every
  "Courier / 12 pt / page numbers" report-format reference to the current ADR-013 brand layout (or a
  bare "per ADR-013" pointer with no duplicated specifics).
- **F59** *(scope contradiction)* — ADR-036 line 177 (2026-06-17) states **income-mtd, income-ytd,
  and postal-by-vendor reports were removed** ("redundant with enhanced Sales and Costs"), but they
  are still fully specified as active in **ADR-006** (report table), **ADR-013** (full content
  specs), and **ADR-018 §9** (endpoints `/api/reports/income-mtd|income-ytd|postal-by-vendor`).
  Resolve in one direction and propagate.
- **F60** *(minor)* — ADR-039 line 33 cites "the Config UI in ADR-034 **§2b**," but ADR-034 numbers
  its sections 1–20 in a table (no "§2b"). Imprecise cross-ref.
- **Clean / strong (no findings):** ADR-078 (communications — thorough, compliance-aware), ADR-080
  (shipping module), ADR-083 / ADR-084 (AI shot list / dimension — well-specified). All cite the
  shot taxonomy via ADR-072 (rolled into F21) and use the "Implemented (WS-…)" status form (F6).
- **F61** *(extends F49 — canonical tab set)* — The **tab bar is defined differently in ≥4 places**:
  ADR-080 §4 lists **11** tabs (no Expenses); ADR-024 §1 routing has **12** segments (incl.
  `expenses`); ADR-024 says "9 tabs" (line 13) and "10 tabs" (line 69); ADR-009 lists 9. Define the
  canonical tab list **once** (Dashboard · Sales · Shipping · Inventory · Receipts · Customers ·
  Vendors · Expenses · Reports · Tutorial & Tips · Outstanding · Config = 12) and reconcile
  ADR-009 / ADR-024 / ADR-080 + ui-design.md §1/§2.
- **F62** *(root-cause mechanism — use as the fix checklist)* — Every recent WS-ADR (079, 080, 081,
  082, 083, 084, 078, and 085) ends with a **"Cross-references to update when [WS] is implemented"**
  list, written in future tense. The work shipped (statuses say Implemented; WS tickets are in
  `completed/`), but **several listed propagations were never executed** — exactly the drift this
  audit found (SEMS annotations on ADR-024/030/031/032; report-removal propagation; shot-taxonomy
  relocation; tab-bar reconciliation). **These per-ADR checklists ARE the remediation backlog** —
  walking each one and verifying every item is the fastest way to close the gaps, and the "future
  tense" wording on a shipped ADR is itself a marker to clean up.
- **Clean / correct (no findings):** ADR-042 (unsaved-changes — **correctly updated** to the SEMS
  3-button dialog, confirming ADR-032's 2-button section is the stale one, F54), ADR-062 (inline
  editing), ADR-073 (Etsy API acceptance — comprehensive; only the F25 superseded ADR-023 ref).
- **F63** *(SEMS drift — ironic)* — ADR-076 §5 describes the **Vendors tab as "Master-detail (same
  pattern as Customers)"** (lines 111–126), but ADR-079 says **Vendors was the SEMS pilot (WS-E1)**
  with the full-width-editor-replaces-list scaffold. The pilot's own ADR still documents the
  pre-SEMS layout. Reconcile ADR-076 §5 to SEMS. Its tab list (line 106 — 10 tabs, no Shipping/no
  Expenses) is a **4th distinct tab list** (feeds F61).
- **F64** *(F38 class)* — ADR-076 line 149 lists "**Listing Coach** (`listing-coach/page.tsx`)" as a
  current `VendorPicker` usage site; the Coach/route is removed (ADR-085). Repoint to inventory
  detail / inline create.
- **F65** *(minor)* — ADR-042 line 25 still labels a protected form "Inventory — **listing workshop**
  (ADR-030)"; the workshop is now the consolidated Listing Content section. Cosmetic label refresh.

### Final machine-scan of the remaining small ADRs (040, 041, 043–055, 057–067, 071, 074, 075, 077)

- **Clean (no markers):** 040, 041, 043, 044, 046, 047, 048, 049, 050, 051, 052, 053, 055, 057, 058,
  063, 064, 065, 066, 067, 074. **ADR-059 is exemplary** — it explicitly instructs removal of a stale
  "panel on the right" reference (self-correcting). ADR-045 only mentions two-panel layouts
  descriptively in cross-refs.
- **F66** *(user-facing — F38 class)* — **ADR-060 contextual-help tooltips contain stale content for
  retired fields.** The `listing_draft_state` tooltip (line 46) tells users "Listing drafts progress
  through stages: draft → generated/imported → approved → published. Only approved drafts can be
  published" — the removed draft-state machine — and tooltips are scoped to the removed "Listing
  workshop" (lines 46, 55). These render in the UI. Rewrite for the ADR-085 `listing_phase` lifecycle
  (or drop the `listing_draft_state` tooltip entirely).
- **F67** *(SEMS drift — more entities)* — Pre-SEMS screen language persists in: **ADR-061** (line 40
  "Master-detail pages (Sales, Inventory)", line 50 "Two-panel pages (Inventory detail + Listing
  workshop)"), **ADR-077** (line 56 "Layout: **Master-detail** with summary cards" — Expenses is a
  *completed* SEMS entity, WS-E4), and **ADR-071** (master-detail spacing tokens). Reconcile the
  layout language to the SEMS scaffold (ADR-079); ADR-077 should state Expenses is on SEMS.
- **F68** *(internal inconsistency)* — ADR-075 retires `listing-coach.ts`/`improve-listing` at line 75
  ("RETIRED (ADR-085)") but still presents `listing-coach` as an **active** OpenAI call site/example
  at lines 13, 35, 47, and 150. Reconcile the examples to the Generate/refine engine call sites.
- **F69** — ADR-071 (visual design system) §4.3 is titled "Listing `listing_draft_state`" and
  documents the **retired** field's states/badges. Update to `listing_phase` (ADR-081/085) so the
  design system reflects the live status dimension.
- **F58 extended** — ADR-054 line 75 is another instance of the stale "**12pt Courier… page numbers
  centered**" report layout. Add it to the F58 sweep (now ADR-036/038/039/054 + design-decisions §21).

---

## ORDERED FIX LIST

Sequenced so that canonical artifacts are de-duplicated first, then user-facing errors, then the
canonical specs (which downstream docs derive from), then systematic stale-reference cleanup, then
doc-level staleness, then cosmetics. Each item cites the finding IDs above.

### Phase 0 — Remove duplicate / stale artifacts (do first; prevents editing the wrong copy)
1. Delete `documents/adr.zip` (stale 2026-02-23, 23-ADR snapshot). **[F4]**
2. Resolve the duplicate ADR set at `TrudysClassicTreasures/etsy-sales-manager/docs/adr/` (old
   0001–0008). Either delete it or replace with a stub README pointing to `documents/adr/` as
   canonical. **[F3]**
3. Decide the fate of the retired Coach docs `documents/LISTING_COACH_SCOPE.md` and
   `system/tips/Listing_Coach_Guide.md`: delete, or move to an `archive/` folder and update the
   links that still point at them. **[F5]**

### Phase 1 — Fix user-facing errors (highest external impact)
4. `tutorial.md` lines 45 & 66 — replace the Listing Coach instructions with the current ADR-085
   flow (inline "Add new item" + hero photo → Generate Listing → Evaluate Quality → Publish); remove
   the retired "approve before publishing" wording and the `Listing_Coach_Guide.md` link. **[F38]**
5. `knowledge-base-topics-catalog.md` D21–D25 — replace the 5 Coach topics with lifecycle topics
   (Generate, Evaluate Quality, remediation, Publish). **[F38]**
6. `etsy-listing-template-and-requirements.md` §7 and line 112 — retitle/rewrite around the inventory
   lifecycle (ADR-085); drop "(ADR-072)". **[F38]**
6a. **ADR-060 contextual-help tooltips** — rewrite the `listing_draft_state` tooltip (line 46) and the
   "Listing workshop"-scoped tooltips (lines 46, 55) for the ADR-085 `listing_phase` lifecycle, or
   drop the draft-state tooltip. These strings render in the UI. **[F66]**

### Phase 2 — Correct the canonical specs (downstream docs derive from these)
7. **ADR-017 §8 DDL ordering bug** — move the `CREATE INDEX … ON tax_payments / receipts /
   business_expenses` statements (lines 978–982) to *after* those tables are created (lines
   1012–1083) so the canonical DDL runs top-to-bottom without "no such table." **[F16]**
8. **ADR-017 invalid enum** — change the `etsy.publish.default_when_made` example from `before_2004`
   to a value in the §1a canonical `when_made` enum (e.g. `2000_2003`). **[F14]**
9. **ADR-017 index narrative ↔ DDL** — reconcile §7 (claims `orders.shipper`, `vendors.is_active`
   indexes) with the §8 DDL (creates neither). Add the indexes or correct the narrative. **[F17]**
10. **ADR-017 duplicate `### 5f.`** — renumber the second 5f ("Supporting tables") and fix the
    duplicated `-- 5f.` DDL comments; add a narrative entry (or exception note) for `communication_log`
    so "§1–§7 match the DDL" holds. **[F13, F18]**
11. **ADR-013 report action set** — correct line 23 to **Print | Export PDF | Export CSV | Cancel**
    to match edge-case #4, ADR-018 §9, and the implementation. **[F19, F39]**
12. **ADR-013 PDFKit staleness** — update edge-case #1/#2 (lines 121–123) to the HTML/browser-print
    model (no server-side PDFKit 500 for regular reports). **[F10]**
13. **ADR-013 markdown corruption** — fix the stray-asterisk italics in the Invoice section (line 61:
    `ship_to*\*`, `business*name`) and the schema-map table (lines 172–175), and de-duplicate the
    repeated "ship-to fields" / "shipping_cost by shipper" rows. **[F11]**
14. **ADR-021 §8 ↔ ADR-017 §1c** — add `etsy_who_made` and `etsy_shipping_profile_id` to ADR-021's
    required-before-publish list (or mark which doc is canonical). **[F29]**
15. **ADR-007 OAuth scope matrix** — repoint the `listings_w`/`listings_r` rows from ADR-023 to
    ADR-085 / ADR-073 (align with ADR-011's clean scope list). **[F7]**
15a. **Canonicalize the quality bar (RESOLVED — two-number model).** Write one statement: **hard gate
    = `listing.min_quality_score`, default 85** — no item publishes below it; 85 is a firm minimum
    (some items legitimately top out there; publishing at exactly 85 is allowed); the gate does
    **not** auto-climb. The AI helps raise the score **toward a ~100 aspiration**, but exceeding 85
    is not required to publish. **Delete the "98" advisory** and **remove ADR-016's "rise toward
    ≈98%"** language. Propagate to ADR-082 (also drop the retired-ADR-068 citation, F40), ADR-081 §1,
    ADR-085 §4, ADR-016 §7. **[F40, F42]**

### Phase 2b — SEMS reconciliation (the second consolidation; finish what ADR-079 promised)
15b. **ADR-079 status** — change "Rollout to remaining entities (WS-E2..E6) pending" to reflect the
    completed rollout (WS-E2–E6 are in `tickets/completed/`; Orders migrated per git history). Update
    §6 likewise. **[F43]**
15c. **ADR-030** — reconcile to SEMS (ADR-079 already says "scaffolding now follows SEMS"): replace the
    Modal create flow (line 177) with the "+ Add New" first-row inline editor; replace the
    list+detail-panel-below layout with the editor-replaces-list scaffold; replace the two-button
    dirty dialog (line 162) with the three-button Save/Discard/Keep (ADR-079 §4 / ADR-042). Keep
    ADR-030's field/validation tables (those stay authoritative). **[F44]**
15d. **ADR-031** — add the same SEMS annotation it never got (it has the ADR-080 one): scaffold now
    follows SEMS; replace "master-detail layout" + Modal create with the SEMS list/editor/sticky-save
    pattern. Keep field definitions authoritative. Also fix the `thank-you` → `thank-you-note`
    endpoint name (line 174). **[F45, F46]**
15e. **Annotate ADR-024 and ADR-062** to point at ADR-079 as the canonical screen pattern (ADR-079
    §Reconciliation lists these as pending annotations). **[F43–F45]**
15f. **ADR-076 (Vendors) and ADR-077 (Expenses)** — reconcile their "Master-detail" layout sections
    to the SEMS scaffold; both are *completed* SEMS pilots/rollouts (WS-E1 Vendors, WS-E4 Expenses).
    Repoint ADR-076's "Listing Coach" VendorPicker usage (line 149) to inventory detail / inline
    create. **[F63, F64, F67]**
15g. **ADR-061** (mobile) and **ADR-071** (visual design) — update "Master-detail / Two-panel /
    Listing workshop" layout language to the SEMS scaffold; ADR-071 §4.3 "Listing `listing_draft_state`"
    → `listing_phase` (ADR-081/085). **[F67, F69]**
15h. **ADR-024 §3.3 / §5** — sweep the stale component list (removed Listing Workshop panel,
    `ListingAuthoringPanel`, `PublishPreview`, modes/approve) and reconcile the file tree + tab count
    with §1 routing. **[F48, F49, F50]**
15i. **ADR-032** — remove the "Reject listing draft" / "Approve draft" entries (retired) and update the
    Unsaved-changes guard to the 3-button SEMS dialog (point to ADR-042/079). **[F54]**
15j. **ADR-037** — fix the API response example to the nested pagination envelope; reconcile the §"DB
    schema" `entity_type` list (line 52) with the authoritative §A1 set. **[F55, F56]**
15k. **Canonical tab bar** — define the 12-tab list once and reconcile ADR-009 / ADR-024 (lines 13,
    69) / ADR-076 (line 106) / ADR-080 §4 + ui-design.md §1/§2. **[F61, F49]**

### Phase 3 — Systematic stale-reference cleanup (supersession not propagated)
16. **Relocate the photo shot-type taxonomy** out of removed ADR-072 into a live ADR (083 and/or
    082, as ADR-072's own note already promises), then repoint every live referrer: ADR-017
    (`picture_classifications`), ADR-030 (line 107), ADR-033 (line 76), ADR-082 §8a header,
    ADR-083, ADR-084. **[F21, F30, F41, F47]**
17. **ADR-083 / ADR-084** — drop "Listing Coach (ADR-072)" as a current UI surface; keep the
    inventory detail picture area (ADR-030/033). **[F22]**
18. **ADR-035 deep link** — reconcile `openWorkshop=1` → "listing workshop panel (ADR-030, ADR-072)"
    with the consolidated inventory-detail listing section (panel + ADR-072 both removed). **[F23]**
19. **ADR-070 scope matrix** — remove/annotate the superseded "Listing workshop (manual/AI/portable)
    + approve + publish | ADR-023" row; keep the ADR-085 row. **[F24]**
20. **ADR-017 retained-column source refs** — update `picture_classifications` (→ taxonomy's new
    home), `listing_category_path` and the six `listing_*` strategy fields from "(ADR-023)" to
    "(ADR-085)". **[F15]**
21. **ADR-020 Type 8** — update the prose definition (line 101) to `listing_phase` values to match
    its own (already-updated) SQL; reorder Type 8 after Type 7 in the body. **[F27, F28]**
22. **ADR-016** — trim the stale "last 20 / limit=20" from the superseded activity-widget paragraph
    (line 121). **[F12]**

### Phase 3b — Report-spec consistency
22a. **Stale report layout sweep** — replace the "12pt Courier / page numbers centered" layout
    references (which ADR-013 superseded with Crimson Text + Raleway + brand banner) in **ADR-036
    (147), ADR-038 (141), ADR-039 (90), ADR-054 (75)** and `design-decisions §21` with a bare "per
    ADR-013" pointer. **[F58]**
22b. **Report-scope contradiction** — resolve whether income-mtd / income-ytd / postal-by-vendor are
    removed (ADR-036 line 177) or active (ADR-006 / ADR-013 / ADR-018 §9), and propagate one answer.
    **[F59]**
15l. **ADR-075** — reconcile the active `listing-coach` call-site examples (lines 13/35/47/150) with
    its own retirement note (line 75). **[F68]**

### Phase 4 — Doc-level staleness
23. `design-decisions-implementation.md` §8 (four-vs-three actions — align to four) and §21 (replace
    the wrong Courier/page-number layout summary with the ADR-013 brand layout or a bare pointer).
    **[F33, F34]**
24. `implementation-guide.md` — rewrite Phase 4 + the "Where to look" listing row around ADR-085/
    081/082; fix the Phase 6 action set; add a "baseline only — see PROGRAM_2026-06-21" note (or new
    phases) for ADR-074/076/077/078/079/080/081–085. **[F35, F36, F37]**
25. `development-plan.md`, `frontend-architecture.md`, `no-developer-questions-build.md`,
    `listing-authoring-task-cards.md` — sweep for ADR-023/068/072 + Listing Coach and repoint to
    ADR-085 (completed-ticket files and dated audit snapshots may be left as historical record).
    **[F5, F35]**
26. `documents/README.md` ("71 ADRs") and `DOC_FUNCTIONAL_UX_COVERAGE_AUDIT.md` ("69 ADRs") — update
    to 85. **[F1, F2]**

### Phase 5 — Cosmetic / consistency (low risk, do last)
27. Standardize the **Status** field vocabulary to the documented keywords (`Accepted | Deprecated |
    Superseded by ADR-XXX`), moving "implemented (WS-…)" / "updated …" / "made canonical …" detail to
    a separate line. Affects 034, 079, 080, 081, 083, 084. **[F6]**
28. Collapse ADR-023's double status line ("Superseded…" + "Accepted (historical)") to one. **[F26]**
29. Add a one-line "historical reference only" banner at the Decision/API sections of superseded
    ADRs 023/068/072 (bodies are still present-tense). **[F32]**
30. ADR-018 Appendix A — renumber the scrambled examples (13 before 12; B21→B29). **[F20]**
31. Fix the stale type-number cross-ref in historical ADR-072 ("ADR-020 Type 9" → Type 8) if
    touched. **[F31]**

---

## Coverage note

Deep full reads (≈45 ADRs): 001–033 (all), 034–039, 042, 056, 060*, 062, 068, 069, 072, 073, 076,
078–084; plus the SSOT index, implementation-guide, ui-design / frontend-architecture excerpts, and
the key supporting docs. Machine-verified across **all 85 ADRs + all supporting docs**: status/date,
superseded-ADR reference map, Listing-Coach reference map, broken-link & non-existent-ADR-number
checks, report-action-set phrasing, stale counts, duplicate-artifact detection, plus a final
marker-scan (Coach/draft_state/approve/master-detail/Courier/flat-pagination) across the remaining
small ADRs (040, 041, 043–055, 057–067, 071, 074, 075, 077). Targeted reads of every flagged item.

**Coverage is now effectively complete.** ADRs with **no findings** (clean/strong): 025, 028, 029,
040, 041, 043–053, 055, 056, 057, 058, 059, 063–067, 069, 073, 074, 078, 080, 083, 084, plus 042/062.
Everything else has at least one logged finding.

**Three owner-confirmed themes drove the deep pass** (and resolve the ambiguities): (1) SEMS is the
single screen pattern → SEMS-drift cluster (F43–F47, F63, F67); (2) AI is the primary author, the
Coach is gone → listing-supersession cluster (F5, F21–F26, F38, F48, F54, F64, F66, F68); (3) the
quality score is the gate, driven toward ~100 → F40/F42. **F62** notes the fastest remediation route:
each recent WS-ADR carries its own "cross-references to update when implemented" checklist — those
lists ARE the backlog, and several items on them were never executed.

Total findings: **69** (F1–F69), plus several positive confirmations of well-maintained ADRs.

Remaining gap for 100% line coverage: the routine UI/feature ADRs **024, 025–029, 032–067 (minus
those read), 069, 071, 073–078, 080, 083–085** were swept, not line-edited. Most likely to hold more
minor instances of the same categories (SEMS annotations pending per ADR-079; shot-taxonomy referrers;
status-vocabulary). ADR-024/028/042/062 specifically should be checked against SEMS (fix 15e).
